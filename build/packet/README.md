# Ngoni Issue #1 V5 Starter Packet

Issue context: `ngoni` is adding a **first V5 packet scaffold** for the string-family pathway with explicit authority gates.

## Scope

- Keep this as an L2 packet starter only.
- Establish the first V5 `family-spec.csv` rows for string-speaking-length and string transfer boundaries.
- Add a short capture/gate scaffold in `validation-loop.csv` and `validation.csv`.
- Register visual/design artifacts in `visual-output-register.csv` with non-authoritative roles.

## File map

- `build/packet/family-spec.csv` — string-family starter rows and authority status markers.
- `build/packet/validation-loop.csv` — measurement and gating sequence for scale, bridge, and body-coupling checks.
- `build/packet/validation.csv` — open gates for V5 readiness.
- `build/packet/visual-output-register.csv` — artifact authority map and non-authoritative visual/design references.
- `build/packet/design.md` — assignment summary + gate sequence.
- `build/packet/README.md` — this file map and role definition.

## Status

- L2 V5 build-packet candidate only.
- No geometry claims are treated as fabrication authority until scoped measurements are collected.
